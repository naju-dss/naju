# Official Mamba-1 (mamba-ssm) baseline; d_state=16, d_conv=4, expand=2 are
# fixed at the official defaults in models/fla_baselines.py.
model: mamba
kind: mamba
d_model: 256
n_layers: 4
dropout: 0.1

lr: 3.0e-4
weight_decay: 1.0e-4
epochs: 50
warmup_ratio: 0.05
batch_size: 32
grad_clip: 1.0
